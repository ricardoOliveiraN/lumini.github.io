# A linha abaixo determina que o diretório node_modules não será enviado ao repositório remoto
node_modules
package-lock.json

# Arquivos de ambiente locais não devem ser versionados
.env
.env.*
!.env.example
!.env.*.example

.github
